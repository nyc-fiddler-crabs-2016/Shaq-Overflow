this is the show page for your question

<%= form_for @question do |f|%>
<%= f.label :title %>
<%= f.text_field :title, value: @question.title %>
<%= f.label :content %>
<%= f.text_area :content, value: @question.content %>
<%= f.submit 'submit' %>
<% end %>
